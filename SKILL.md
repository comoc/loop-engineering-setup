---
name: loop-engineering-setup
description: >
  ループエンジニアリング(Loop Engineering / Addy Osmani 2026)の実践のためのセットアップを一気通貫で行うスキル。
  対象リポジトリを診断し、ループ設計書(検証可能なゴール・検証ゲート・Maker/Checker分離・停止条件・
  人間確認ポイント)を提示し、承認後に .claude/agents/ のサブエージェント定義、/loop コマンド、
  状態ファイル、判定基準、CLAUDE.md 追記など実行に必要な一式を生成して試運転まで行う。
  ユーザーが「ループエンジニアリング」「ループを回したい」「エージェントに自走させたい」「自律ループ」
  「maker-checker」「自動で直し続けて」「放置で回るようにしたい」などに言及したら、スキル名を
  明示しなくても必ずこのスキルを使うこと。既にループ関連ファイルがあるリポジトリの診断・改善にも使う。
---

# Loop Engineering Setup — ループ実践のためのセットアップ

## 背景と目的

ループエンジニアリングとは「自分がエージェントにプロンプトを打つのをやめ、プロンプトを打つシステム
自体を設計する」手法 (Addy Osmani, 2026年6月)。原文: "Loop engineering is replacing yourself as
the person who prompts the agent."

実践には6つの構成要素と、なにより「客観的に落とせる検証ゲート」が要る:
Automations(自動起動) / Worktrees(並列隔離) / Skills(知識の外部化) / Connectors(外部連携) /
Sub-agents(Maker-Checker分業) / Memory(ディスク上の状態)。

このスキルは対象リポジトリでそのセットアップを一気通貫で行う: **診断 → 設計(承認) → 生成 → 試運転 → 運用ガイド**。

生成する全ファイルに反映すべき大原則:

1. **書いた者に採点させない** — Maker と Checker は別エージェント・別権限。生成モデルの自己採点は構造的に甘い。
2. **エージェントは忘れる、リポジトリは忘れない** — 状態はコンテキストではなくディスクに置く。
3. **完了は自己申告でなく機械検証ゲートで判定する** — 「テスト全パスかつlintクリーン」のように、別の主体が真偽を確認できる形にする。
4. **人間確認はゼロにせず、基準を明文化して濃淡をつける** — 可逆性×影響範囲で自動採用〜同期承認を分ける。
5. **停止条件は多層に** — 回数上限・無進展検出・連続失敗・予算。放置されたループは放置されたまま間違え続ける。

**無人実行時の規定(全Phase共通)**: ユーザーが不在・応答しない場合、承認ポイントでは推奨デフォルト
を採用し、成果物の冒頭に「未承認・推奨デフォルト採用」と明記して進む。ただし同期承認必須級の操作
(不可逆・影響範囲が広いもの、Automations の有効化)は無人では実行しない。

## Phase 1 — 診断

リポジトリを調べ、ループを回せる状態かを見立てる:

- **検証ゲートの実在(最重要)**: テストコマンドを特定し実際に走らせて現状を確認。lint / format /
  型チェック / ビルドの有無も確認。動くゲートが1つもなければ、**ゲート整備こそ最初のセットアップ**である
  — 検証なしのループは高速に間違いを量産する装置でしかない。
- **仕様・判断材料**: docs/ や README に、Checker が実装と突き合わせられる「何が正しいか」の記述があるか。
- **既存の Claude Code 資産**: CLAUDE.md、.claude/agents|commands|skills、hooks 設定。
- **Connectors**: 環境側で使える MCP・連携と、リポジトリが前提とする外部システム(Issueトラッカー、CI等)の両方を見る。
- **git 状態**: クリーンか、デフォルトブランチ、worktree の使える構成か。

結果は診断レポートとして提示し、`docs/loop/REPORT.md` に保存する(提示だけでは会話とともに消える。
原則2「状態はディスクに」は自分の成果物にも適用する):

```
## ループ診断レポート
| 項目 | 現状 | 評価 | 最初の一手 |
|---|---|---|---|
| 検証ゲート | pytest 63件パス / lintなし | B | ruff 導入 |
| Automations | なし | F | 試運転後に検討 |
| Worktrees | 利用可 | A | — |
| Skills/知識 | CLAUDE.md なし | D | 本スキルで生成 |
| Connectors | GitHub MCP あり | B | — |
| Sub-agents | なし | F | 本スキルで生成 |
| Memory/状態 | なし | F | 本スキルで生成 |
```

(検証ゲートは6要素に加えて必ず1行目に置く。ループの成立可否を最も左右するため。)

評価の目安: A=そのまま運用可 / B=軽微な整備で可 / C=整備すれば使える / D=実質未整備 / F=不在。
評点それ自体より「最初の一手」欄が具体的であることを優先する。

## Phase 2 — 設計

**題材選定**: 最初のループ題材の適性は3軸 — 反復性 / 検証可能性 / 経済価値。初回は小さく客観検証
できるもの(バグ修正、テスト補完、lint解消、ドキュメントと実装の同期など)を2〜3件提案してユーザーと
決める。本番デプロイ・セキュリティ対応・不可逆操作は初回題材にしない。ユーザーが不在の無人実行時は
最小の題材を選び、その前提を明記して進む。

**ループ設計書**を提示して承認を得てから生成に進み、`docs/loop/DESIGN.md` に保存する:

```
## ループ設計書: <題材名>
- ゴール(検証可能な完了条件): 例「pytest 全パス かつ ruff エラー0 かつ Checker の High 指摘 0件」
  ※「良くする」「改善する」のような曖昧なゴールは書かない。
- 検証ゲート: 実行するコマンド列と合格基準
- Maker: 調査と修正を実装する
- Checker: 判定のみ。tools は Read, Grep, Glob, Bash(検証コマンド再実行用)に限定し、
  Write / Edit は持たせない(修正を物理的に禁止)
- 反復: Maker 修正 → ゲート実行 → Checker 独立検証 → 指摘あれば差し戻し
- 停止条件: 反復N回上限(推奨5) / 2回連続で改善なし(=ゲート不合格数も High 指摘数も減らない
  イテレーションが2回続く。機械判定できる定義にする) / ツール連続失敗 —
  いずれかで status を "requires human review" にして停止し、要約を状態ファイルに残す
- 人間確認の濃淡(可逆性 × 影響範囲):
  - 戻せる × 影響が狭い → 自動採用し事後サマリで報告
  - 戻せない または 影響が広い → 実行前に同期承認を必須にする
- 状態ファイル: docs/loop/STATE.md に試行・ゲート結果・未解決・次の一手を記録
```

## Phase 3 — 生成

承認された設計に基づき一式を生成する。既存ファイルがある場合は上書きせず差分を提案する。

1. **`.claude/agents/loop-checker.md`** — 判定専用。frontmatter の tools から Write/Edit を除外。
   Bash は検証コマンドの再実行のみに使い、ファイルを変更するコマンド(sed -i、リダイレクト書き込み、
   git commit 等)は禁止する旨を定義本文に明記する(tools 制限だけでは Bash 経由の編集を防げない。
   機械的に強制したければ hooks での deny も提案してよい)。過去のレビュー履歴や Maker の報告を
   鵜呑みにせず、ログ・コード・仕様・テストを自分で再確認する(fresh context での独立検証)。
   報告スキーマを定義: High(ゲート不合格 or 仕様との矛盾)/ Medium / Low、および根拠。
2. **`.claude/agents/loop-maker.md`** — 実装担当。最初のステップに「エラーの性質判定」を置く:
   「想定内の異常系(対応不要)」と「本当のバグ(修正対象)」の判別基準を題材に合わせて明文化する。
   エラー=バグではない。修正はテストファーストを推奨: 先に失敗テストを追加して RED を確認してから
   直す(ゲートが本当にその欠陥を落とせることの実証になる)。スコープ規律: 無関係な問題は直さず
   STATE.md の backlog に記録して報告。
3. **`.claude/commands/loop.md`** — 1イテレーションを回すオーケストレータのスラッシュコマンド:
   状態読込 → Maker → ゲート実行 → Checker → 状態更新 → 継続 / 完了 / エスカレーションの判定。
   Maker / Checker をサブエージェントとして呼ぶのはこのコマンドの実行主体であり、STATE.md の更新
   責務もここが持つ(Checker は書き込めないため、判定結果の転記はオーケストレータの仕事)。
4. **`docs/loop/STATE.md`** — 状態テンプレート: status(running | done | requires human review)、
   試行履歴、ゲート結果、未解決事項、backlog、次の一手。
5. **`docs/loop/criteria.md`** — 完了条件・High の定義・エラー性質判定・人間確認マトリックスを1枚に。
6. **CLAUDE.md 追記** — 検証コマンド、ループ運用ルール、状態ファイルの場所。既存内容は壊さない
   (ファイルがなければ新規作成してよい)。
7. **(任意・明示承認時のみ) Automations** — cron / GitHub Actions / スケジュールタスクの雛形を提示
   する。**勝手に有効化しない**: 置いただけで有効になる場所(`.github/workflows/` 等)には置かず、
   `docs/loop/automations.sample.md` のような不活性な形で残す。トリガー自動化は実コストが発生する
   ため、試運転が安定してからユーザー自身が有効化する。

## Phase 4 — 試運転(スモークループ)

生成しただけで終わらせない。tools の記載漏れのような設定ミスは座学では見つからず、実行して初めて
見つかる。

- 最小の実題材で1イテレーションだけ実行する。
- 確認事項: Checker が Maker の結論を再検証したか / ゲートは実際に「落とせる」か(意図的に失敗を
  作って確認してよい) / STATE.md が更新されたか。
- 停止・エスカレーション判定は1回の反復では発火しないため、ドライランで確認する: STATE.md を
  人工的に上限直前・無進展2回の状態に書き換えて /loop の判定ロジックが "requires human review"
  に落とすことを確かめ、確認後に元へ戻す。
- 見つかった不備を修正し、結果を報告する。

## Phase 5 — 運用ガイド

最後に以下を簡潔に伝える:

- **回し方**: まず /loop を都度実行。安定したら Automations を有効化し、L1(報告のみ) →
  L2(承認付き修正) → L3(無人実行) の順で段階的に自律度を上げる。
- **コストガード**: 回数上限・無進展検出・予算上限を設計書どおりに保つ。緩めるのは実測してから。
- **残る責任**: 検証の責任は人間に残る。ループが生んだコードを読むこと。理解負債(生成量と理解の
  差)と認知投降(中身を見ずに信頼する状態)は静かに蓄積する。
- **並列化**: 複数ループを同時に回したくなったら、先に worktree で作業空間を隔離する。

## してはいけないこと

- 検証ゲートなしでループを組む
- Checker に Write/Edit を与える。Maker の自己申告で完了扱いにする
- 曖昧なゴールのまま生成に進む
- ユーザーの明示承認なしに Automations や hooks を有効化する
- 1つの題材の対応中に無関係な問題までスコープを広げる(backlog に記録して報告する)
